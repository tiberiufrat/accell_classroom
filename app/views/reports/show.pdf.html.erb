<section class="section" style="border:none; background-color: white">
  <div class="section-body" style="border:none">
    <div class="card" style="border:none">
      <div class="card-header" style="border:none">
        <h3 style="font-family: 'Comfortaa', cursive;">accell</h3>
        <h3 class="text-center"><%= t('reports.pdf_heading') %></h3>
        <p class="text-center lead" style="font-family: 'Source Code Pro', monospace; margin-bottom: 0">№. <u>&nbsp;&nbsp; <%= @report.id %> &nbsp;&nbsp;</u></p>
      </div>
      <div class="card-body" style="border:none">
        <p class="lead text-center">Eliberat pentru <strong><%= @report.user.name %></strong>.</p>
        <p class="">Prezentul raport a fost generat automat și cuprinde activitățile realizate pe Google Classroom în perioada: <br/> <strong><%= l @report.date_start, format: :date %></strong> – <strong><%= l @report.date_end, format: :date %></strong>.</p>

        <div class="list-group" style="border:none">
          <% @activities.each do |activity| %>
            <a href="#" class="list-group-item list-group-item-action flex-column align-items-start">
              <div class="d-flex w-100 justify-content-between">
                <h6 class="mb-1"><%= activity.title %></h6>
                <span><%= l(activity.time, format: :long) %></span>
              </div>
              <p class="mb-1"><%= activity.description.truncate(100, separator: ' ', omission: '... (se continuă)') %></p>
              <span><%= t('reports.in_course') %> <%= activity.course.name %></span>
            </a>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <footer class="text-center">Acest raport a fost generat automat de  <span style="font-family: 'Comfortaa', cursive; font-size: 1.1em">accell</span>, unde dezvoltăm tehnologie pentru educație.<br/>Vizitați-ne și aflați mai multe pe accell.ro</footer>
</section>
