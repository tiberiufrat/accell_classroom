<!DOCTYPE html>
<html lang="<%= I18n.default_locale %>">
  <head>
    <meta charset="UTF-8">
    <meta content="width=device-width, initial-scale=1, maximum-scale=1, shrink-to-fit=no" name="viewport">
    <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@700&family=Source+Code+Pro&display=swap" rel="stylesheet">
    <title><%= page_title %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'mobile', media: 'all', 'data-turbolinks-track': 'reload' if platform.mobile_app? %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div id="app">
      <div class="main-wrapper main-wrapper-1">
        <div class="navbar-bg"></div>
        <%= render 'shared/navbar' %>
        <%= render 'shared/sidebar' %>
        <div class="main-content">
          <%= content_for?(:content) ? yield(:content) : yield %>
        </div>
        <%# render 'shared/footer' %>
      </div>
    </div>
    <%= render 'shared/toastr' %>
  </body>
</html>
